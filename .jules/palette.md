## 2026-05-03 - Added ARIA labels to Auth inputs and Dark mode toggle
**Learning:** Found some inputs without id and correct htmlFor label connections, and missing ARIA attributes.
**Action:** Always verify all form inputs have proper ids and labels with matching htmlFor, or use aria-label. Found an input in ModuleCard without accessible label.
