## 2024-05-02 - AuthModal Input Labels
**Learning:** Auth forms in this app's existing components sometimes lack explicit `htmlFor` and `id` mappings, breaking screen reader associations and reducing click targets.
**Action:** Always check form fields for correct `htmlFor`/`id` pairs in newly reviewed forms or auth components.
